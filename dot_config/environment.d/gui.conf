# Make Qt apps (Telegram, VLC, etc.) use native Wayland
QT_QPA_PLATFORM=wayland

# Enable native Wayland back-end for Mozilla apps (Firefox, Thunderbird)
MOZ_ENABLE_WAYLAND=1

# Optional but harmless: remind GDK to favour Wayland (GTK 3 apps, libadwaita)
# GDK_BACKEND=wayland
