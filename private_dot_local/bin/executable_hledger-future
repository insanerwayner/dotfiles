#!/usr/bin/sh

futuretotals()
    {
    hledger bal Assets:BofTX \
        --forecast -p "every 14 days from $(hledger-payday -1) to $(hledger-payday 25)" \
        -H --pretty-tables --transpose --auto "$@"
    }

futuretotals "$@"
